---
alwaysApply: true
---
 امنیت، پایداری، کارایی
- CORS: فقط دامنه‌های اجازه‌داده‌شده از env.
- نرخ پیام روزانه: slowapi + Redis؛ کلید = user_id یا IP (برای کاربر ناشناس).
- ورودی‌ها را sanitize کن؛ فیلدهای متنی را trim کن.
- محدودیت اندازه بدنه (Body size) و timeout معقول تنظیم کن.
- لاگ ساختاریافته JSON (logger مرکزی). هیچ اطلاعات حساس در لاگ.
